#include "list.h"
#include <stdio.h>
/**
 * add - funtion that adds
 * a - first integer
 * b - second integer
 * Return : Answer
 */

int add(int a, int b)
{
	return (a + b);
}


/**
 * sub - funtion that adds
 * a - first integer
 * b - second integer
 * Return : Answer
 */

int sub(int a, int b)
{
        return (a - b);
}

/**
 * mul - funtion that adds
 * a - first integer
 * b - second integer
 * Return : Answer
 */

int mul(int a, int b)
{
        return (a * b);
}

/**
 * div - funtion that adds
 * a - first integer
 * b - second integer
 * Return : Answer
 */

int div(int a, int b)
{
        return (a / b);
}

/**
 * mod - funtion that adds
 * a - first integer
 * b - second integer
 * Return : Answer
 */

int mod(int a, int b)
{
        return (a % b);
}
