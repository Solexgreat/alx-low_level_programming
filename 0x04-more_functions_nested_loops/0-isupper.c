#include "main.h"

/**
 * _isupper-a function that checks for uppercase character
 * @c : character to check the case
 * Return: Always 0
 */
int _isupper(int c)
{
	_isupper(c);

	if (c >= 65 && c <= 90)
	{
		return (1);
	}
	else
	{
		return (0);
	}
}
