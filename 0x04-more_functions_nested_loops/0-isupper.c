#include "main.h"

/**
 * _isupper-a function that checks for uppercase character
 * @c : character to check the case
 * Return: 0 or 1
 */
int _isupper(int c)
{
	int _isupper(c);

	if (c >= 65 && c <= 90)
	{
		return (1);
	}
	return (0);
}
