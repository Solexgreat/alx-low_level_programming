#include <stdio.h>
#include <stdlib.h>

/**
 * main - prints the last digit of the random
 * number stored in the variable n
 * Return: Always 0 (Success)
 */
int main(void)
{
	int n;

	n = 0;

	while (n < 10)
	{
		printf("%i\n", n);
	}
	n++;
	return (0);
}
